import 'package:flutter/material.dart';

class CommandDialog extends StatelessWidget {
  const CommandDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
